- if object.errors.any?
  .bg-red-100.border-red-500.px-4.py-3.rounded
    %h4.text-red-700
      = pluralize(object.errors.count, "エラー")
      が発生しました:
    %ul
      - object.errors.full_messages.each do |message|
        %li= message
