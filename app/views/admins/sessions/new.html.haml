%h1.text-3xl.font-bold.my-4.text-center ログイン

%div{"class" => "container flex justify-center"}
  %div{"class" => "w-2/5 mx-auto"}
    = simple_form_for resource, as: resource_name, url: session_path(resource_name), html: { class: 'space-y-6' } do |f|
      = render 'users/shared/error_messages', object: resource

      .form-inputs.space-y-4
        = f.input :email, required: false, autofocus: true,
          input_html: { class: "w-full p-2 border border-gray-500 rounded focus:ring focus:ring-indigo-500", autocomplete: "email" },
          wrapper_html: { class: 'block' }, label_html: { class: 'block mb-1' }

        = f.input :password, required: false,
          input_html: { class: "w-full p-2 border border-gray-500 rounded focus:ring focus:ring-indigo-500", autocomplete: "current-password" },
          wrapper_html: { class: 'block' }, label_html: { class: 'block mb-1' }

        - if devise_mapping.rememberable?
          = f.input :remember_me, as: :boolean

      .form-actions.flex.justify-center
        = f.button :submit, 'ログイン', class: submit_button_class
